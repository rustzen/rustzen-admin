[package]
name = "rustzen-config"
version = "0.1.0"
edition = "2024"

[dependencies]
figment = { version = "0.10.19", features = ["env"] }
once_cell = "1.21"
rustzen-runtime = { path = "../runtime" }
serde = { version = "1", features = ["derive"] }
