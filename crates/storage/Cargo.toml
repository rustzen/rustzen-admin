[package]
name = "rustzen-storage"
version = "0.5.0"
edition = "2024"

[dependencies]
sqlx = { version = "0.9.0", features = [
    "runtime-tokio",
    "sqlite",
    "chrono",
    "uuid",
    "migrate",
] }
